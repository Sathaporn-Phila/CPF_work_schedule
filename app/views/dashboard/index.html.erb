<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title> index </title>
    </head>
    <body>
        <header>
            <div class="d-flex flex-row-reverse align-items-center">
    
                <%=link_to logout_path do%>
                    <i class="bi bi-box-arrow-right btn btn-danger" id="logout-button" aria-hidden="true"> Logout </i>
                <%end%>
                
                <h1 class='flex-grow-1 text-center' id="header-text" text-align="center">CPF schedule workplan</h1>
            </div>
        </header>
        <% if @current_user.role =="ผู้จัดการ"%>
            <%= render "manager" %>
        <% elsif @current_user.role =="หัวหน้าแผนก"%>
            <%= render "cheif" %>
        <% elsif @current_user.role =="คนงานทั่วไป"%>
            <%= render "worker" %>
        <% end%>

    </body>
</html>